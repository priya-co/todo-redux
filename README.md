props.todoLists.map((list) => (
    <div>
        <div className="label">{list.label}</div>
    </div>
))


props.todoLists.map((list) => {
    // statements
    return <div>

    </div>
})


props.todoLists.map((list) => ());
